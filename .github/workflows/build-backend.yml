# Build and push Backend Docker image on changes to backend or docker/backend
name: Build Backend Docker Image

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'backend/**'
      - 'docker/backend/**'
      - '.github/workflows/build-backend.yml'
  pull_request:
    branches:
      - main
      - develop
    paths:
      - 'backend/**'
      - 'docker/backend/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Backend image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/backend/Dockerfile
          push: false
          load: true
          tags: taskflow-backend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # Optional: push to registry on main (uncomment and set secrets)
      # - name: Login to Docker Hub
      #   if: github.ref == 'refs/heads/main'
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}
      # - name: Push Backend image
      #   if: github.ref == 'refs/heads/main'
      #   run: |
      #     docker tag taskflow-backend:latest ${{ secrets.DOCKER_USERNAME }}/taskflow-backend:latest
      #     docker push ${{ secrets.DOCKER_USERNAME }}/taskflow-backend:latest
